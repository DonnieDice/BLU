## v7.1.1

### Framework Migration - Utility Deduplication
- Removed local `DeepCopy` — already provided by `RGX:DeepCopy()`
- Removed local `Throttle` — already provided by `RGX:Throttle(key, seconds, func)`
- Removed local `Debounce` — already provided by `RGX:Debounce(key, seconds, func)`
- Removed local `SafeCall` — replaced by `RGX:QueueForCombat()`

### Framework Migration - Sound Muter
- Rewrote `sounds.lua` to use `RGX:GetSound():MuteList(ids)` / `UnmuteList(ids)`
- Sound ID list stays BLU-specific; muting mechanism delegated to RGX-Framework

### Fixes
- Fixed syntax error in `sound_panel.lua` — extra `end` from dropdown migration orphaned `elseif`
