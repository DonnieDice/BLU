## v8.0.0-alpha.2

- fix: resolve user custom sound manifest entries through the shared custom-sound path resolver
  - `BLU_UserCustomSounds` entries now behave like `/blu addcustom`
  - short filenames can resolve from BLU local search paths such as `media\\sounds`
  - profile-stored custom sound entries are normalized through the same resolution path on refresh
- goal: make custom MP3s and other loose user sound files show up reliably in the combat sound picker
