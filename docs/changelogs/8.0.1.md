## v8.0.1

### Fixes

- **Combat music now loops correctly** — replaced the broken timer-based replay with WoW's native `PlayMusic`/`StopMusic` API so the track loops seamlessly for the full duration of combat regardless of file length.
